var arr = [23,13,12,11,16,14,10,1000];

// arr.sort();     //we can assign sorted array to a new variable or not.

// console.log(arr);


// arr.sort((o1,o2)=>o1-o2)  //sort array in ascending

// console.log(arr);

// arr.sort((o1,o2)=>o2-o1)   //sort array in descending

// console.log(arr);